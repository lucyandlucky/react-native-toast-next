import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: 'black',
    borderRadius: 5,
    padding: 10,
    opacity: 0.7,
  },
  text: {
    color: '#FFFFFF',
    fontSize: 15,
    paddingTop: 10,
    paddingBottom: 10,
    paddingStart: 20,
    paddingEnd: 20,
  },
});
